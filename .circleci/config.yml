version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
      - machine-test

jobs:
  machine-test:
    machine: true
    resource_class: large
    steps:
      - checkout
      - run: npm install
      - run: node index.js
  runner-test:
    machine: true
    resource_class: brentmmarks/docker-runner
    steps:
      - checkout
      - run: npm install
      - run: node index.js
